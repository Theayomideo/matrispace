% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/differential-expression.R
\name{find_spatially_corrected_ecm_markers}
\alias{find_spatially_corrected_ecm_markers}
\title{Find spatially corrected ECM markers between two groups}
\usage{
find_spatially_corrected_ecm_markers(
  seurat_obj,
  group.by,
  ident.1,
  ident.2,
  assay = NULL,
  layer = "data",
  min.pct = 0.05,
  logfc.threshold = 0.25,
  wilcox.padj.threshold = 0.05,
  categories = NULL,
  sp_topfrac = 1,
  verbose = TRUE
)
}
\arguments{
\item{seurat_obj}{A Seurat object}

\item{group.by}{Metadata column defining the comparison groups}

\item{ident.1}{Group of interest}

\item{ident.2}{Reference group}

\item{assay}{Assay to use. If NULL, auto-detects.}

\item{layer}{Expression layer to test. Defaults to \code{"data"}.}

\item{min.pct}{Minimum fraction of spots expressing the gene in either group.
Defaults to \code{0.05}.}

\item{logfc.threshold}{Minimum absolute log2 fold-change required to send a
gene to the spatial model. Defaults to \code{0.25}.}

\item{wilcox.padj.threshold}{Adjusted p-value threshold for Wilcoxon
pre-screening. Defaults to \code{0.05}.}

\item{categories}{Optional matrisome categories to restrict testing to.}

\item{sp_topfrac}{Optional fraction of Wilcoxon-significant genes to pass to
\pkg{spaMM}, ranked by adjusted p-value. Defaults to \code{1}.}

\item{verbose}{Print progress messages}
}
\value{
Data frame containing Wilcoxon statistics, spaMM statistics, and
matrisome annotations.
}
\description{
Runs a two-stage workflow designed for within-sample comparisons where
spatial autocorrelation matters. ECM genes are first screened with a
Wilcoxon rank-sum test, then significant hits are re-tested with a
\pkg{spaMM} spatial mixed model using Matern covariance.
}
\details{
This function mirrors the differential expression strategy used in the
MatriSpace case study: Wilcoxon pre-screening on ECM genes followed by
spatial correction with \code{spaMM::fitme(gene_expr ~ group + Matern(1 | x + y))}.
}
\examples{
\dontrun{
spatial_de <- find_spatially_corrected_ecm_markers(
  seurat_obj,
  group.by = "CompositionCluster_CC",
  ident.1 = "CC1",
  ident.2 = "CC3"
)
}

}
