% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ligand-receptor.R
\name{score_lr_activity}
\alias{score_lr_activity}
\title{Calculate spatial L-R activity scores}
\usage{
score_lr_activity(
  seurat_obj,
  lr_db = NULL,
  adj_matrix = NULL,
  assay = NULL,
  layer = "data",
  verbose = TRUE
)
}
\arguments{
\item{seurat_obj}{A Seurat object}

\item{lr_db}{Ligand-receptor database with 'Ligand' and 'Receptor' columns.
Defaults to package data.}

\item{adj_matrix}{Sparse adjacency matrix for spatial neighbors.
If NULL, computed from coordinates.}

\item{assay}{Assay to use (default: "SCT")}

\item{layer}{Layer within assay (default: "data")}

\item{verbose}{Print progress messages}
}
\value{
Seurat object with "LRACTIVITY" assay added
}
\description{
Computes spatially-weighted ligand-receptor co-expression scores for each
spot. The score represents incoming paracrine signal strength.
}
\details{
\subsection{Scoring Formula}{

\deqn{Score_{spot} = \sqrt{Receptor_{spot}} \times \sum_{neighbors} \sqrt{Ligand_{neighbor}}}
\subsection{Why This Formula?}{

\strong{Product Structure:} Requires BOTH ligand AND receptor for signaling.
If either is zero, score is zero (biological AND gate).

\strong{Square Root Transformation:}
\itemize{
\item Variance stabilization for skewed expression data
\item Approximates receptor saturation kinetics
\item Geometric mean principle - robust to outliers
}
}

}
}
\examples{
\dontrun{
seurat_obj <- score_lr_activity(seurat_obj)
}

}
