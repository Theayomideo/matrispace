% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/differential-expression.R
\name{find_ecm_de_samples}
\alias{find_ecm_de_samples}
\title{Differential expression of ECM genes between samples or conditions}
\usage{
find_ecm_de_samples(
  seurat_obj,
  condition.col,
  condition.1,
  condition.2,
  cluster.col = NULL,
  assay = NULL,
  test.use = "wilcox",
  min.pct = 0.05,
  logfc.threshold = 0.25,
  categories = NULL,
  pseudobulk = FALSE,
  sample.col = NULL,
  blocking.col = NULL,
  min.cells.per.sample = 10,
  verbose = TRUE
)
}
\arguments{
\item{seurat_obj}{A Seurat object}

\item{condition.col}{Metadata column containing condition/sample labels}

\item{condition.1}{First condition to compare}

\item{condition.2}{Second condition to compare}

\item{cluster.col}{Optional metadata column for cluster-aware analysis.
In spot-level mode, DE is run separately within each cluster. In
pseudobulk mode, counts are aggregated at the sample-by-cluster level and
cluster is added to the DESeq2 design.}

\item{assay}{Assay to use. If NULL, auto-detects.}

\item{test.use}{Statistical test: "wilcox" (default), "MAST", "DESeq2", "LR"}

\item{min.pct}{Minimum fraction expressing (default: 0.05)}

\item{logfc.threshold}{Minimum log2 fold change (default: 0.25)}

\item{categories}{Matrisome categories to include. If NULL, uses all ECM genes.}

\item{pseudobulk}{If TRUE, aggregates to pseudo-bulk per sample before DE.
Recommended for multi-sample experiments.}

\item{sample.col}{Required if pseudobulk=TRUE. Column identifying individual samples.}

\item{blocking.col}{Optional metadata column added to the DESeq2 design in
pseudobulk mode to control for paired subjects, batches, or other
blocking effects.}

\item{min.cells.per.sample}{Minimum cells per sample for pseudo-bulk (default: 10)}

\item{verbose}{Print progress messages}
}
\value{
Data frame with DE results. If cluster.col is provided, includes
a 'cluster' column for cluster-stratified results.
}
\description{
Compares ECM gene expression between different samples or experimental conditions.
Supports both spot-level and pseudo-bulk analysis approaches.
}
\details{
\subsection{Analysis Modes}{

\strong{Spot-level (pseudobulk=FALSE):}
Treats each spot as independent observation. Fast but may inflate
significance due to pseudo-replication within samples.

\strong{Pseudo-bulk (pseudobulk=TRUE):}
Aggregates counts by sample, then compares between conditions with DESeq2.
When \code{cluster.col} is supplied, profiles are aggregated at the
sample-by-cluster level and cluster is included in the design. Use
\code{blocking.col} to adjust for repeated subjects or batch effects.
}

\subsection{Cluster-Stratified Analysis}{

When cluster.col is provided, DE is run separately within each cluster.
This reveals cluster-specific expression changes between conditions.
}
}
\examples{
\dontrun{
# Basic comparison between conditions
de_results <- find_ecm_de_samples(seurat_obj,
                                   condition.col = "condition",
                                   condition.1 = "tumor",
                                   condition.2 = "normal")

# Cluster-stratified analysis
stratified_de <- find_ecm_de_samples(seurat_obj,
                                      condition.col = "condition",
                                      condition.1 = "tumor",
                                      condition.2 = "normal",
                                      cluster.col = "cell_type")

# Pseudo-bulk analysis (recommended for multiple samples)
pseudobulk_de <- find_ecm_de_samples(seurat_obj,
                                      condition.col = "condition",
                                      condition.1 = "treated",
                                      condition.2 = "control",
                                      pseudobulk = TRUE,
                                      sample.col = "sample_id",
                                      blocking.col = "patient_id")
}

}
