% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ecm-annotation.R
\name{annotate_ecm_niches}
\alias{annotate_ecm_niches}
\title{Annotate ECM niches using ScType}
\usage{
annotate_ecm_niches(seurat_obj, markers_path = NULL)
}
\arguments{
\item{seurat_obj}{A Seurat object with SCT assay}

\item{markers_path}{Path to Excel file with ECM niche markers.
If NULL, uses built-in markers from inst/extdata.}
}
\value{
Seurat object with \code{ecm_niche} column in metadata
(and \code{ecm_domain_annotation} retained as an alias for backward
compatibility).
}
\description{
Classifies each spot into an ECM niche (e.g. Interstitial, Basement
membrane) using the ScType algorithm with ECM-specific marker genes.
}
\details{
The function requires scaled SCT data. If the object lacks an SCT assay,
run \code{Seurat::SCTransform()} first.
}
\examples{
\dontrun{
seurat_obj <- annotate_ecm_niches(seurat_obj)
table(seurat_obj$ecm_niche)
}

}
